[project]
name = "mlipx-hub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlipx",
]

[tool.uv.sources]
mlipx = { git = "https://github.com/basf/mlipx" }
